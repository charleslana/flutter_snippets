import 'package:flutter/material.dart';

class SnippetContainer extends StatelessWidget {
  const SnippetContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.blue,
      ),
    );
  }
}
