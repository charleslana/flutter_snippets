import 'package:flutter/material.dart';

class SnippetText extends StatelessWidget {
  const SnippetText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Texto Normal'),
      ),
    );
  }
}
